<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>연락처 목록</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
        integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>

<body>
    <!-- include header.ejs -->
    <%- include('../include/header') %>

    <!-- Main -->
    <main id="site-main">
        <div class="button-box">
            <a href="/contact/register" class="btn btn-success">연락처 추가</a>
        </div>

        <table class="table">
            <thead>
                <tr>
                    <th>이름</th>
                    <th>메일 주소</th>
                    <th>전화번호</th>
                    <th>&nbsp;</th>
                </tr>
            </thead>
            <tbody>
                <% contacts.forEach(contact=> { %>
                    <tr>
                        <td><a href="/contact/<%= contact._id %>"><%= contact.name %></a></td>
                        <td><%= contact.email %></td>
                        <td><%= contact.phone %></td>
                        <td><a href="/contact/edit/<%= contact._id %>" class="btn btn-outline-success btn-sm">수정</a>
                            <a href="/contact/delete/<%= contact._id %>" class="btn btn-outline-danger btn-sm">삭제</a>
                        </td>
                    </tr>
                    <% }); %>

            </tbody>
        </table>
    </main>
    <!-- /Main -->

</body>

</html>